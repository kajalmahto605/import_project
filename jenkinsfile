pipeline {
    agent any

    stages {
        stage('Parse YAML from File') {
            steps {
                script {
                    def config = readYaml file: 'config.yml'
                    echo "Parsed: ${config}"
                    echo "Name: ${config.name}"
                    echo "Tools: ${config.tools}"
                }
            }
        }
    }
}
